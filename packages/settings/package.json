{
  "name": "@penny/settings",
  "version": "1.0.0",
  "description": "Package that deals with settings",
  "scripts": {
    "build": "yarn install && tsc index.ts",
    "test": "jest --coverage"
  },
  "dependencies": {
    "@penny/db-models": "1.0.0"
  }
}
